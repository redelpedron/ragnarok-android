2026-09-04T15:51:55.9191111Z ##[group]Run export PATH=$PATH:$(go env GOPATH)/bin
export PATH=$PATH:$(go env GOPATH)/bin
cd $GITHUB_WORKSPACE/go-bridge
gomobile build -androidapi 34 -target=android/arm64 -o $GITHUB_WORKSPACE/goro-raw.apk .
shell: /usr/bin/bash -e {0}
env:
  ANDROID_HOME: /usr/local/lib/android/sdk
  ANDROID_SDK_ROOT: /usr/local/lib/android/sdk
  ANDROID_NDK_HOME: /opt/hostedtoolcache/ndk/r26b/x64
gomobile: manual declaration of uses-sdk in AndroidManifest.xml not supported
Process completed with exit code 1.